# 3D 场景预留目录

这里用于放置五魔方 3D 建模相关代码。当前已经包含一个静态五魔方原型：

- `MegaminxCanvas.tsx`：React Three Fiber 画布、灯光、阴影和轨道控制。
- `MegaminxModel.tsx`：基于正十二面体方向排布的 12 个五边形彩色面。

建议后续按以下方向拆分：

- `geometry/`：五魔方几何体、面片和块体数据。
- `controls/`：相机控制、层转动交互和动画状态。
- `materials/`：颜色、材质和高亮规则。

当前模型仍是视觉原型，不包含真实块体切分、层转动分组和状态映射。
